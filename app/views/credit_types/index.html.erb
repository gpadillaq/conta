<p id="notice"><%= notice %></p>

<h1>Credit Types</h1>

<table>
  <thead>
    <tr>
      <th>Descripcion</th>
      <th>Interez</th>
      <th>Activo</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @credit_types.each do |credit_type| %>
      <tr>
        <td><%= credit_type.descripcion %></td>
        <td><%= credit_type.interez %></td>
        <td><%= credit_type.activo %></td>
        <td><%= link_to 'Show', credit_type %></td>
        <td><%= link_to 'Edit', edit_credit_type_path(credit_type) %></td>
        <td><%= link_to 'Destroy', credit_type, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Credit Type', new_credit_type_path %>

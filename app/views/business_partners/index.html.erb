<% content_for :table do %>
  <table class="table table-hover" id="business_partners">
    <thead>
      <tr>
        <th>Nombre</th>
        <th>Tipo</th>
        <th>Moneda</th>
        <th>Telefono</th>
        <th>Celular</th>
        <th>Email</th>
        <th>Activo</th>
        <th>Direccion</th>
        <th>Identificador</th>
        <th colspan="3"></th>
      </tr>
    </thead>
    <tbody>
      <% @records.each do |business_partner| %>
        <tr>
          <td><%= business_partner.name %></td>
          <td><%= business_partner.business_partner_type.display_name %></td>
          <td><%= business_partner.coin.display_name %></td>
          <td><%= business_partner.telephone %></td>
          <td><%= business_partner.cellphone %></td>
          <td><%= business_partner.email %></td>
          <td><%= business_partner.active %></td>
          <td><%= business_partner.direction %></td>
          <td><%= business_partner.id_number %></td>
          <td><%= link_to 'Ver', business_partner %></td>
          <td><%= link_to 'Editar', edit_business_partner_path(business_partner) %></td>
          <td><%= link_to 'Eliminar', business_partner, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% end %>

<%= render 'shared/index', page_name: 'Socios de Negocios', url: new_business_partner_path %>
